// we are importing this version of history of ours in the app.js file as history.

import { createBrowserHistory } from "history"; // this package is installed directly along with react-router-dom;

export default createBrowserHistory();
